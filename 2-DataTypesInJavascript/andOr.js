//AND(&&)
let x = 5;
let valMin = 0,
  valMax = 10;

if (x >= valMin && x <= valMax) {
  console.log("In range");
} else {
  console.log("Out of range");
}
//OR(||)

let vacations = true,
  restDay = false;
if (vacations || restDay) {
  console.log("The father can assist to the game");
} else {
  console.log("The father cannot assist");
}
